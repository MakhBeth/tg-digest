# tg-digest Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SPA in-browser che sincronizza gruppi Telegram (GramJS user account), salva i messaggi in IndexedDB e genera digest all'apertura + risposte Q&A via LLM (Ollama locale o Anthropic).

**Architecture:** Nessun backend. GramJS via WebSocket per Telegram, Dexie per lo storage, un client LLM unico con due provider, React con Context + useLiveQuery per la UI. Digest generato all'apertura per i gruppi con messaggi nuovi.

**Tech Stack:** Vite, React 18, TypeScript, Dexie + dexie-react-hooks, telegram (GramJS), react-markdown, CSS Modules + theme.css, @fontsource, @hugeicons/react, vite-plugin-pwa, Vitest.

**Spec:** `docs/superpowers/specs/2026-08-25-tg-digest-design.md`

## Global Constraints

- **NIENTE COMMIT AUTOMATICI**: l'utente fa i commit a mano. Ogni task termina con un checkpoint "pronto per commit", mai con `git commit`.
- React 18 (`react@^18.2.0`), TypeScript 5, Vite 5+.
- Niente Tailwind, niente router, niente store esterni: CSS Modules + theme.css, stato di vista locale, dati via `useLiveQuery`.
- Solo testo dai messaggi Telegram; media resi come `[media]`.
- Tutte le stringhe UI in italiano.
- Non usare em-dash nei testi.
- Test: Vitest, solo sulle parti pure (`src/lib/**`). GramJS e fetch LLM mockati.
- Comandi: `npm run dev`, `npm run build`, `npm test` (vitest run), `npm run lint` (tsc --noEmit).

---

### Task 1: Scaffold progetto

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/App.module.css`, `src/styles/theme.css`, `src/vite-env.d.ts`, `.gitignore`
- Test: nessuno (verifica: build passa)

**Interfaces:**
- Produces: progetto avviabile con `npm run dev`, `npm test` funzionante, token CSS in `theme.css` usati da tutti i task UI.

- [ ] **Step 1: Init progetto e dipendenze**

```bash
cd ~/Projects/tg-digest
npm create vite@latest . -- --template react-ts
npm i react@^18.2.0 react-dom@^18.2.0 dexie dexie-react-hooks telegram react-markdown @fontsource/dm-sans @fontsource/space-mono @hugeicons/react @hugeicons/core-free-icons
npm i -D vitest vite-plugin-pwa vite-plugin-node-polyfills @types/node fake-indexeddb
```

Se `npm create vite` si rifiuta perché la cartella non è vuota (c'è `docs/`), scaffoldare in una temp dir e copiare i file dentro, senza toccare `docs/`.

`vite-plugin-node-polyfills` serve perché GramJS in browser richiede `Buffer`/`process`.

- [ ] **Step 2: Config**

`vite.config.ts`:

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { nodePolyfills } from 'vite-plugin-node-polyfills'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    nodePolyfills({ globals: { Buffer: true, process: true } }),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: 'tg-digest',
        short_name: 'tg-digest',
        theme_color: '#0f1115',
        display: 'standalone',
      },
    }),
  ],
  test: { environment: 'node' },
})
```

In `package.json` scripts: `"test": "vitest run"`, `"lint": "tsc --noEmit"`, `"build": "tsc && vite build"`. Aggiungere `/// <reference types="vitest" />` in cima a `vite.config.ts`.

- [ ] **Step 3: theme.css e App shell**

`src/styles/theme.css` (token, dark di default, stile essenziale):

```css
:root {
  --bg: #0f1115;
  --surface: #171a21;
  --surface-2: #1f232d;
  --border: #2a2f3a;
  --text: #e8eaf0;
  --text-dim: #9aa1b0;
  --accent: #6ea8fe;
  --danger: #ff6b6b;
  --radius: 10px;
  --space-1: 4px; --space-2: 8px; --space-3: 16px; --space-4: 24px; --space-5: 40px;
  --font-body: 'DM Sans', system-ui, sans-serif;
  --font-mono: 'Space Mono', monospace;
}
[data-theme='light'] {
  --bg: #f6f7f9; --surface: #ffffff; --surface-2: #eef0f4;
  --border: #d8dce3; --text: #1a1d24; --text-dim: #5c6470;
}
* { box-sizing: border-box; }
body { margin: 0; background: var(--bg); color: var(--text); font-family: var(--font-body); }
button { font: inherit; cursor: pointer; }
input, textarea, select { font: inherit; background: var(--surface-2); color: var(--text); border: 1px solid var(--border); border-radius: var(--radius); padding: var(--space-2) var(--space-3); }
```

`src/main.tsx` importa `@fontsource/dm-sans`, `@fontsource/space-mono`, `./styles/theme.css` e monta `<App />`. `src/App.tsx` per ora rende `<h1>tg-digest</h1>`.

- [ ] **Step 4: Verifica**

Run: `npm run lint && npm run build && npm test`
Expected: lint ok, build ok, vitest esce con "no test files" (accettabile) o 0 test.

- [ ] **Step 5: Checkpoint utente**

Segnalare: scaffold pronto, l'utente può fare `git init` + primo commit.

---

### Task 2: lib/db (Dexie, schema, pruning)

**Files:**
- Create: `src/types/models.ts`, `src/lib/db/db.ts`, `src/lib/db/settings.ts`, `src/lib/db/pruning.ts`
- Test: `src/lib/db/pruning.test.ts`, `src/lib/db/settings.test.ts`

**Interfaces:**
- Produces:
  - `db` istanza Dexie con tabelle `groups`, `messages`, `summaries`, `settings`
  - tipi `Group`, `Message`, `Summary`, `LlmProvider`
  - `getSetting(key: SettingKey): Promise<string | undefined>`, `setSetting(key, value: string)`, `getSettings(): Promise<AppSettings>`
  - `pruneOldMessages(now: number, retentionDays: number): Promise<number>` (ritorna quanti cancellati)

- [ ] **Step 1: Tipi e schema**

`src/types/models.ts`:

```ts
export type LlmProvider = 'ollama' | 'anthropic'

export interface Group {
  id: string            // chat id Telegram come stringa
  title: string
  followed: boolean
  lastSyncedMsgId: number
  lastDigestAt: number  // epoch ms, 0 = mai
}

export interface Message {
  groupId: string
  msgId: number
  date: number          // epoch ms
  senderName: string
  text: string
  replyToMsgId?: number
}

export interface Summary {
  id?: number
  groupId: string | null
  type: 'digest' | 'question'
  question?: string
  periodFrom: number
  periodTo: number
  text: string
  model: string
  createdAt: number
}

export interface AppSettings {
  provider: LlmProvider
  model: string
  anthropicKey: string
  ollamaUrl: string
  profile: string
  retentionDays: number
}
```

`src/lib/db/db.ts`:

```ts
import Dexie, { type Table } from 'dexie'
import type { Group, Message, Summary } from '../../types/models'

export interface SettingRow { key: string; value: string }

export class TgDigestDb extends Dexie {
  groups!: Table<Group, string>
  messages!: Table<Message, [string, number]>
  summaries!: Table<Summary, number>
  settings!: Table<SettingRow, string>

  constructor() {
    super('tg-digest')
    this.version(1).stores({
      groups: 'id',
      messages: '[groupId+msgId], date, groupId',
      summaries: '++id, createdAt, groupId',
      settings: 'key',
    })
  }
}

export const db = new TgDigestDb()
```

- [ ] **Step 2: Test settings (failing)**

`src/lib/db/settings.test.ts` (usare `fake-indexeddb`):

```ts
import 'fake-indexeddb/auto'
import { describe, it, expect } from 'vitest'
import { getSettings, setSetting } from './settings'

describe('settings', () => {
  it('ritorna i default quando vuoto', async () => {
    const s = await getSettings()
    expect(s.provider).toBe('ollama')
    expect(s.ollamaUrl).toBe('http://localhost:11434')
    expect(s.retentionDays).toBe(30)
  })
  it('legge quello che scrive', async () => {
    await setSetting('provider', 'anthropic')
    await setSetting('model', 'claude-haiku-4-5-20251001')
    const s = await getSettings()
    expect(s.provider).toBe('anthropic')
    expect(s.model).toBe('claude-haiku-4-5-20251001')
  })
})
```

Run: `npm test` → FAIL (modulo mancante).

- [ ] **Step 3: Implementazione settings**

`src/lib/db/settings.ts`:

```ts
import { db } from './db'
import type { AppSettings } from '../../types/models'

export type SettingKey = keyof AppSettings

export const DEFAULT_SETTINGS: AppSettings = {
  provider: 'ollama',
  model: 'gemma3',
  anthropicKey: '',
  ollamaUrl: 'http://localhost:11434',
  profile: '',
  retentionDays: 30,
}

export async function getSetting(key: SettingKey): Promise<string | undefined> {
  return (await db.settings.get(key))?.value
}

export async function setSetting(key: SettingKey, value: string): Promise<void> {
  await db.settings.put({ key, value })
}

export async function getSettings(): Promise<AppSettings> {
  const rows = await db.settings.toArray()
  const map = Object.fromEntries(rows.map(r => [r.key, r.value]))
  return {
    ...DEFAULT_SETTINGS,
    ...map,
    retentionDays: map.retentionDays ? Number(map.retentionDays) : DEFAULT_SETTINGS.retentionDays,
  } as AppSettings
}
```

- [ ] **Step 4: Test pruning (failing) e implementazione**

`src/lib/db/pruning.test.ts`:

```ts
import 'fake-indexeddb/auto'
import { describe, it, expect } from 'vitest'
import { db } from './db'
import { pruneOldMessages } from './pruning'

const DAY = 86_400_000

describe('pruneOldMessages', () => {
  it('cancella solo i messaggi oltre la retention', async () => {
    const now = 100 * DAY
    await db.messages.bulkPut([
      { groupId: 'g1', msgId: 1, date: now - 40 * DAY, senderName: 'a', text: 'vecchio' },
      { groupId: 'g1', msgId: 2, date: now - 5 * DAY, senderName: 'b', text: 'recente' },
    ])
    const deleted = await pruneOldMessages(now, 30)
    expect(deleted).toBe(1)
    expect(await db.messages.count()).toBe(1)
    expect((await db.messages.toArray())[0].msgId).toBe(2)
  })
})
```

`src/lib/db/pruning.ts`:

```ts
import { db } from './db'

export async function pruneOldMessages(now: number, retentionDays: number): Promise<number> {
  const cutoff = now - retentionDays * 86_400_000
  return db.messages.where('date').below(cutoff).delete()
}
```

- [ ] **Step 5: Verifica e checkpoint**

Run: `npm test && npm run lint` → PASS. Checkpoint utente per commit.

---

### Task 3: lib/llm (client, prompt, troncamento)

**Files:**
- Create: `src/lib/llm/format.ts`, `src/lib/llm/prompts.ts`, `src/lib/llm/client.ts`
- Test: `src/lib/llm/format.test.ts`, `src/lib/llm/prompts.test.ts`, `src/lib/llm/client.test.ts`

**Interfaces:**
- Consumes: `Message`, `AppSettings` da Task 2.
- Produces:
  - `formatMessages(msgs: Message[]): string` (righe `[gg/mm hh:mm] Nome: testo`)
  - `truncateToBudget(msgs: Message[], maxChars: number): { msgs: Message[]; truncated: boolean }` (taglia dal più vecchio)
  - `buildDigestPrompt(args: { groupTitle: string; formatted: string; profile: string }): { system: string; user: string }`
  - `buildQuestionPrompt(args: { formatted: string; question: string; profile: string; truncated: boolean }): { system: string; user: string }`
  - `chatCompletion(settings: AppSettings, system: string, user: string): Promise<string>`

- [ ] **Step 1: Test format + truncate (failing)**

`src/lib/llm/format.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { formatMessages, truncateToBudget } from './format'
import type { Message } from '../../types/models'

const msg = (msgId: number, text: string, date = 1735000000000): Message =>
  ({ groupId: 'g', msgId, date, senderName: 'Ada', text })

describe('formatMessages', () => {
  it('formatta una riga per messaggio', () => {
    const out = formatMessages([msg(1, 'ciao')])
    expect(out).toMatch(/^\[\d{2}\/\d{2} \d{2}:\d{2}\] Ada: ciao$/)
  })
})

describe('truncateToBudget', () => {
  it('non tocca nulla se sotto budget', () => {
    const r = truncateToBudget([msg(1, 'a'), msg(2, 'b')], 10_000)
    expect(r.truncated).toBe(false)
    expect(r.msgs).toHaveLength(2)
  })
  it('taglia dal più vecchio quando sfora', () => {
    const msgs = [msg(1, 'x'.repeat(600)), msg(2, 'y'.repeat(600))]
    const r = truncateToBudget(msgs, 700)
    expect(r.truncated).toBe(true)
    expect(r.msgs).toHaveLength(1)
    expect(r.msgs[0].msgId).toBe(2)
  })
})
```

Run: `npm test` → FAIL.

- [ ] **Step 2: Implementazione format**

`src/lib/llm/format.ts`:

```ts
import type { Message } from '../../types/models'

export function formatMessages(msgs: Message[]): string {
  return msgs.map(m => {
    const d = new Date(m.date)
    const p = (n: number) => String(n).padStart(2, '0')
    return `[${p(d.getDate())}/${p(d.getMonth() + 1)} ${p(d.getHours())}:${p(d.getMinutes())}] ${m.senderName}: ${m.text}`
  }).join('\n')
}

export function truncateToBudget(msgs: Message[], maxChars: number): { msgs: Message[]; truncated: boolean } {
  const sorted = [...msgs].sort((a, b) => a.date - b.date)
  let total = sorted.reduce((n, m) => n + m.text.length + m.senderName.length + 20, 0)
  let i = 0
  while (total > maxChars && i < sorted.length - 1) {
    total -= sorted[i].text.length + sorted[i].senderName.length + 20
    i++
  }
  return { msgs: sorted.slice(i), truncated: i > 0 }
}
```

- [ ] **Step 3: Test + implementazione prompt**

`src/lib/llm/prompts.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { buildDigestPrompt, buildQuestionPrompt } from './prompts'

describe('prompts', () => {
  it('digest include titolo gruppo e profilo', () => {
    const p = buildDigestPrompt({ groupTitle: 'Productized', formatted: '[x] A: ciao', profile: 'UX engineer' })
    expect(p.user).toContain('Productized')
    expect(p.user).toContain('UX engineer')
    expect(p.user).toContain('[x] A: ciao')
  })
  it('question segnala il troncamento', () => {
    const p = buildQuestionPrompt({ formatted: 'f', question: 'q?', profile: '', truncated: true })
    expect(p.user).toContain('troncat')
  })
})
```

`src/lib/llm/prompts.ts`:

```ts
const SYSTEM = 'Sei un assistente che analizza conversazioni di gruppi Telegram. Rispondi in italiano, in markdown, conciso e concreto. Cita i nomi degli autori quando rilevante.'

export function buildDigestPrompt(args: { groupTitle: string; formatted: string; profile: string }) {
  const profilePart = args.profile
    ? `\nSegnala in una sezione a parte le cose rilevanti per questo profilo: ${args.profile}`
    : ''
  return {
    system: SYSTEM,
    user: `Riassumi la conversazione del gruppo "${args.groupTitle}". Evidenzia: thread principali, decisioni, link e risorse condivise.${profilePart}\n\nMessaggi:\n${args.formatted}`,
  }
}

export function buildQuestionPrompt(args: { formatted: string; question: string; profile: string; truncated: boolean }) {
  const profilePart = args.profile ? `\nProfilo di chi chiede: ${args.profile}` : ''
  const truncPart = args.truncated
    ? '\nNota: i messaggi più vecchi del periodo sono stati troncati per limiti di contesto; dillo nella risposta.'
    : ''
  return {
    system: SYSTEM,
    user: `Rispondi alla domanda basandoti solo sui messaggi qui sotto.${profilePart}${truncPart}\n\nDomanda: ${args.question}\n\nMessaggi:\n${args.formatted}`,
  }
}
```

- [ ] **Step 4: Test + implementazione client**

`src/lib/llm/client.test.ts` (mock di `fetch`):

```ts
import { describe, it, expect, vi, afterEach } from 'vitest'
import { chatCompletion } from './client'
import { DEFAULT_SETTINGS } from '../db/settings'

afterEach(() => vi.unstubAllGlobals())

describe('chatCompletion', () => {
  it('ollama: usa /v1/chat/completions e ritorna il testo', async () => {
    const fetchMock = vi.fn(async () => new Response(JSON.stringify({
      choices: [{ message: { content: 'risposta' } }],
    })))
    vi.stubGlobal('fetch', fetchMock)
    const out = await chatCompletion({ ...DEFAULT_SETTINGS, provider: 'ollama', model: 'gemma3' }, 'sys', 'user')
    expect(out).toBe('risposta')
    expect(fetchMock.mock.calls[0][0]).toBe('http://localhost:11434/v1/chat/completions')
  })
  it('anthropic: usa /v1/messages con header browser access', async () => {
    const fetchMock = vi.fn(async () => new Response(JSON.stringify({
      content: [{ type: 'text', text: 'ok' }],
    })))
    vi.stubGlobal('fetch', fetchMock)
    const out = await chatCompletion({ ...DEFAULT_SETTINGS, provider: 'anthropic', model: 'claude-haiku-4-5-20251001', anthropicKey: 'k' }, 'sys', 'user')
    expect(out).toBe('ok')
    const [url, init] = fetchMock.mock.calls[0]
    expect(url).toBe('https://api.anthropic.com/v1/messages')
    expect((init!.headers as Record<string, string>)['anthropic-dangerous-direct-browser-access']).toBe('true')
  })
  it('errore HTTP: lancia con status e corpo', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => new Response('boom', { status: 500 })))
    await expect(chatCompletion(DEFAULT_SETTINGS, 's', 'u')).rejects.toThrow(/500/)
  })
})
```

`src/lib/llm/client.ts`:

```ts
import type { AppSettings } from '../../types/models'

export async function chatCompletion(settings: AppSettings, system: string, user: string): Promise<string> {
  if (settings.provider === 'anthropic') {
    const res = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'content-type': 'application/json',
        'x-api-key': settings.anthropicKey,
        'anthropic-version': '2023-06-01',
        'anthropic-dangerous-direct-browser-access': 'true',
      },
      body: JSON.stringify({
        model: settings.model,
        max_tokens: 2048,
        system,
        messages: [{ role: 'user', content: user }],
      }),
    })
    if (!res.ok) throw new Error(`Anthropic ${res.status}: ${await res.text()}`)
    const data = await res.json()
    return data.content.filter((b: { type: string }) => b.type === 'text').map((b: { text: string }) => b.text).join('')
  }
  const res = await fetch(`${settings.ollamaUrl}/v1/chat/completions`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({
      model: settings.model,
      messages: [{ role: 'system', content: system }, { role: 'user', content: user }],
    }),
  })
  if (!res.ok) throw new Error(`Ollama ${res.status}: ${await res.text()}`)
  const data = await res.json()
  return data.choices[0].message.content
}

export const CONTEXT_CHAR_BUDGET = 60_000
```

- [ ] **Step 5: Verifica e checkpoint**

Run: `npm test && npm run lint` → PASS. Checkpoint utente per commit.

---

### Task 4: lib/telegram (GramJS wrapper)

**Files:**
- Create: `src/lib/telegram/service.ts`, `src/lib/telegram/session.ts`
- Test: nessun unit test (wrapper sottile su GramJS); verifica manuale in Task 6.

**Interfaces:**
- Consumes: `Message` da Task 2.
- Produces (`src/lib/telegram/service.ts`):
  - `class TelegramService` singleton esportato come `telegramService` con:
    - `isAuthorized(): Promise<boolean>` (connette se serve)
    - `startLogin(apiId: number, apiHash: string, phone: string): Promise<void>` (invia il codice)
    - `completeLogin(code: string, password?: string): Promise<void>` (salva la session)
    - `listDialogs(): Promise<Array<{ id: string; title: string; isGroup: boolean }>>`
    - `fetchNewMessages(groupId: string, minId: number): Promise<Message[]>` (max 500 per chiamata, dal più vecchio)
    - `logout(): Promise<void>` (disconnette e cancella la session)
  - Errori di auth revocata rilanciati come `TelegramAuthError` (classe esportata)

- [ ] **Step 1: Session storage**

`src/lib/telegram/session.ts`:

```ts
const KEY = 'tg-digest:session'
const CREDS = 'tg-digest:creds'

export const loadSession = (): string => localStorage.getItem(KEY) ?? ''
export const saveSession = (s: string): void => localStorage.setItem(KEY, s)
export const clearSession = (): void => localStorage.removeItem(KEY)

export const loadCreds = (): { apiId: number; apiHash: string } | null => {
  const raw = localStorage.getItem(CREDS)
  return raw ? JSON.parse(raw) : null
}
export const saveCreds = (apiId: number, apiHash: string): void =>
  localStorage.setItem(CREDS, JSON.stringify({ apiId, apiHash }))
```

- [ ] **Step 2: Service**

`src/lib/telegram/service.ts`:

```ts
import { TelegramClient } from 'telegram'
import { StringSession } from 'telegram/sessions'
import type { Message } from '../../types/models'
import { loadSession, saveSession, clearSession, loadCreds, saveCreds } from './session'

export class TelegramAuthError extends Error {}

const AUTH_ERRORS = ['AUTH_KEY_UNREGISTERED', 'SESSION_REVOKED', 'USER_DEACTIVATED']

class TelegramService {
  private client: TelegramClient | null = null
  private pendingPhone = ''
  private phoneCodeHash = ''

  private async getClient(): Promise<TelegramClient> {
    if (this.client?.connected) return this.client
    const creds = loadCreds()
    if (!creds) throw new TelegramAuthError('Credenziali API mancanti')
    this.client = new TelegramClient(new StringSession(loadSession()), creds.apiId, creds.apiHash, {
      connectionRetries: 3,
    })
    await this.client.connect()
    return this.client
  }

  async isAuthorized(): Promise<boolean> {
    if (!loadCreds() || !loadSession()) return false
    try {
      const client = await this.getClient()
      return await client.isUserAuthorized()
    } catch {
      return false
    }
  }

  async startLogin(apiId: number, apiHash: string, phone: string): Promise<void> {
    saveCreds(apiId, apiHash)
    clearSession()
    this.client = null
    const client = await this.getClient()
    this.pendingPhone = phone
    const result = await client.sendCode({ apiId, apiHash }, phone)
    this.phoneCodeHash = result.phoneCodeHash
  }

  async completeLogin(code: string, password?: string): Promise<void> {
    const client = await this.getClient()
    try {
      await client.invoke(new (await import('telegram')).Api.auth.SignIn({
        phoneNumber: this.pendingPhone,
        phoneCodeHash: this.phoneCodeHash,
        phoneCode: code,
      }))
    } catch (e) {
      if (String(e).includes('SESSION_PASSWORD_NEEDED') && password) {
        await client.signInWithPassword(loadCreds()!, {
          password: async () => password,
          onError: (err) => { throw err },
        })
      } else {
        throw e
      }
    }
    saveSession(client.session.save() as unknown as string)
  }

  async listDialogs(): Promise<Array<{ id: string; title: string; isGroup: boolean }>> {
    const client = await this.getClient()
    const dialogs = await client.getDialogs({ limit: 100 })
    return dialogs.map(d => ({
      id: String(d.id),
      title: d.title ?? String(d.id),
      isGroup: Boolean(d.isGroup || d.isChannel),
    }))
  }

  async fetchNewMessages(groupId: string, minId: number): Promise<Message[]> {
    try {
      const client = await this.getClient()
      const raw = await client.getMessages(groupId, { minId, limit: 500, reverse: true })
      return raw
        .filter(m => m.message || m.media)
        .map(m => ({
          groupId,
          msgId: m.id,
          date: (m.date ?? 0) * 1000,
          senderName: (m.sender && 'firstName' in m.sender && m.sender.firstName) || 'Sconosciuto',
          text: m.message || '[media]',
          replyToMsgId: m.replyTo?.replyToMsgId,
        }))
    } catch (e) {
      if (AUTH_ERRORS.some(code => String(e).includes(code))) {
        clearSession()
        throw new TelegramAuthError(String(e))
      }
      throw e
    }
  }

  async logout(): Promise<void> {
    try { await this.client?.disconnect() } catch { /* già disconnesso */ }
    this.client = null
    clearSession()
  }
}

export const telegramService = new TelegramService()
```

Nota per l'implementatore: le firme GramJS vanno verificate contro la versione installata (`node_modules/telegram`); se `sendCode`/`getMessages` differiscono, adattare mantenendo le interfacce pubbliche di questo service invariate.

- [ ] **Step 3: Verifica e checkpoint**

Run: `npm run lint && npm run build` → PASS (la verifica funzionale del login avviene nel Task 6). Checkpoint utente per commit.

---

### Task 5: Sync + digest orchestration

**Files:**
- Create: `src/lib/digest/selectMessages.ts`, `src/lib/digest/runDigest.ts`, `src/lib/digest/sync.ts`
- Test: `src/lib/digest/selectMessages.test.ts`

**Interfaces:**
- Consumes: `db`, `getSettings` (Task 2), `formatMessages`, `truncateToBudget`, `buildDigestPrompt`, `buildQuestionPrompt`, `chatCompletion`, `CONTEXT_CHAR_BUDGET` (Task 3), `telegramService` (Task 4).
- Produces:
  - `selectMessagesForPeriod(groupIds: string[], from: number, to: number): Promise<Message[]>` (ordinati per data)
  - `syncFollowedGroups(): Promise<void>` (per ogni gruppo `followed`: fetch nuovi, bulkPut, aggiorna `lastSyncedMsgId`)
  - `runOpenDigest(now: number): Promise<void>` (digest per ogni gruppo con novità; errori per gruppo isolati)
  - `askQuestion(args: { groupIds: string[]; from: number; to: number; question: string }): Promise<Summary>`

- [ ] **Step 1: Test selezione messaggi (failing) e implementazione**

`src/lib/digest/selectMessages.test.ts`:

```ts
import 'fake-indexeddb/auto'
import { describe, it, expect, beforeEach } from 'vitest'
import { db } from '../db/db'
import { selectMessagesForPeriod } from './selectMessages'

beforeEach(() => db.messages.clear())

describe('selectMessagesForPeriod', () => {
  it('filtra per gruppo e periodo, ordina per data', async () => {
    await db.messages.bulkPut([
      { groupId: 'g1', msgId: 2, date: 200, senderName: 'a', text: 'secondo' },
      { groupId: 'g1', msgId: 1, date: 100, senderName: 'a', text: 'primo' },
      { groupId: 'g2', msgId: 1, date: 150, senderName: 'b', text: 'altro gruppo' },
      { groupId: 'g1', msgId: 3, date: 999, senderName: 'a', text: 'fuori periodo' },
    ])
    const out = await selectMessagesForPeriod(['g1'], 50, 300)
    expect(out.map(m => m.text)).toEqual(['primo', 'secondo'])
  })
})
```

`src/lib/digest/selectMessages.ts`:

```ts
import { db } from '../db/db'
import type { Message } from '../../types/models'

export async function selectMessagesForPeriod(groupIds: string[], from: number, to: number): Promise<Message[]> {
  const msgs = await db.messages
    .where('date').between(from, to, true, true)
    .filter(m => groupIds.includes(m.groupId))
    .toArray()
  return msgs.sort((a, b) => a.date - b.date)
}
```

- [ ] **Step 2: Sync**

`src/lib/digest/sync.ts`:

```ts
import { db } from '../db/db'
import { telegramService } from '../telegram/service'

export async function syncFollowedGroups(): Promise<void> {
  const groups = await db.groups.filter(g => g.followed).toArray()
  for (const g of groups) {
    const msgs = await telegramService.fetchNewMessages(g.id, g.lastSyncedMsgId)
    if (msgs.length === 0) continue
    await db.messages.bulkPut(msgs)
    const maxId = Math.max(...msgs.map(m => m.msgId))
    await db.groups.update(g.id, { lastSyncedMsgId: maxId })
  }
}
```

- [ ] **Step 3: Digest e Q&A**

`src/lib/digest/runDigest.ts`:

```ts
import { db } from '../db/db'
import { getSettings } from '../db/settings'
import { formatMessages, truncateToBudget } from '../llm/format'
import { buildDigestPrompt, buildQuestionPrompt } from '../llm/prompts'
import { chatCompletion, CONTEXT_CHAR_BUDGET } from '../llm/client'
import { selectMessagesForPeriod } from './selectMessages'
import type { Summary } from '../../types/models'

export async function runOpenDigest(now: number): Promise<void> {
  const settings = await getSettings()
  const groups = await db.groups.filter(g => g.followed).toArray()
  for (const g of groups) {
    const from = g.lastDigestAt || now - 7 * 86_400_000
    const msgs = await selectMessagesForPeriod([g.id], from, now)
    if (msgs.length === 0) continue
    const { msgs: kept } = truncateToBudget(msgs, CONTEXT_CHAR_BUDGET)
    const prompt = buildDigestPrompt({ groupTitle: g.title, formatted: formatMessages(kept), profile: settings.profile })
    try {
      const text = await chatCompletion(settings, prompt.system, prompt.user)
      await db.summaries.add({
        groupId: g.id, type: 'digest', periodFrom: from, periodTo: now,
        text, model: `${settings.provider}/${settings.model}`, createdAt: now,
      })
      await db.groups.update(g.id, { lastDigestAt: now })
    } catch (e) {
      await db.summaries.add({
        groupId: g.id, type: 'digest', periodFrom: from, periodTo: now,
        text: `**Errore digest**: ${String(e)}`, model: `${settings.provider}/${settings.model}`, createdAt: now,
      })
      // lastDigestAt NON si aggiorna: al prossimo giro si riprova sullo stesso periodo
    }
  }
}

export async function askQuestion(args: { groupIds: string[]; from: number; to: number; question: string }): Promise<Summary> {
  const settings = await getSettings()
  const msgs = await selectMessagesForPeriod(args.groupIds, args.from, args.to)
  const { msgs: kept, truncated } = truncateToBudget(msgs, CONTEXT_CHAR_BUDGET)
  const prompt = buildQuestionPrompt({ formatted: formatMessages(kept), question: args.question, profile: settings.profile, truncated })
  const text = await chatCompletion(settings, prompt.system, prompt.user)
  const summary: Summary = {
    groupId: args.groupIds.length === 1 ? args.groupIds[0] : null,
    type: 'question', question: args.question,
    periodFrom: args.from, periodTo: args.to,
    text, model: `${settings.provider}/${settings.model}`, createdAt: Date.now(),
  }
  summary.id = await db.summaries.add(summary) as number
  return summary
}
```

- [ ] **Step 4: Verifica e checkpoint**

Run: `npm test && npm run lint` → PASS. Checkpoint utente per commit.

---

### Task 6: Context + Onboarding UI

**Files:**
- Create: `src/context/AppContext.tsx`, `src/components/Onboarding.tsx`, `src/components/Onboarding.module.css`, `src/components/GroupPicker.tsx`, `src/components/GroupPicker.module.css`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `telegramService`, `TelegramAuthError` (Task 4), `db` (Task 2).
- Produces:
  - `AppContext` con `{ view: 'onboarding' | 'home' | 'settings'; setView; authChecked: boolean }` e provider `AppProvider` che al mount chiama `telegramService.isAuthorized()` e imposta la view iniziale (`home` se autorizzato, altrimenti `onboarding`)
  - `<Onboarding />`: wizard a 3 passi (credenziali API → telefono/codice/2FA → scelta gruppi)
  - `<GroupPicker />`: lista dialoghi con toggle; al toggle fa `db.groups.put({ id, title, followed: true, lastSyncedMsgId: 0, lastDigestAt: 0 })` o update `followed: false`

- [ ] **Step 1: AppContext**

`src/context/AppContext.tsx`:

```tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'
import { telegramService } from '../lib/telegram/service'

export type View = 'onboarding' | 'home' | 'settings'

interface AppCtx { view: View; setView: (v: View) => void; authChecked: boolean }

const Ctx = createContext<AppCtx | null>(null)

export function AppProvider({ children }: { children: ReactNode }) {
  const [view, setView] = useState<View>('onboarding')
  const [authChecked, setAuthChecked] = useState(false)
  useEffect(() => {
    telegramService.isAuthorized().then(ok => {
      setView(ok ? 'home' : 'onboarding')
      setAuthChecked(true)
    })
  }, [])
  return <Ctx.Provider value={{ view, setView, authChecked }}>{children}</Ctx.Provider>
}

export function useApp(): AppCtx {
  const ctx = useContext(Ctx)
  if (!ctx) throw new Error('useApp fuori da AppProvider')
  return ctx
}
```

- [ ] **Step 2: Onboarding wizard**

`src/components/Onboarding.tsx`: componente con stato locale `step: 'creds' | 'phone' | 'code' | 'groups'`.

- Step `creds`: input `api_id` (number) e `api_hash` (testo), testo di aiuto con link `https://my.telegram.org/apps` e istruzioni brevi ("Accedi, crea una app, copia api_id e api_hash"). Bottone "Continua" → step `phone`.
- Step `phone`: input telefono internazionale (`+39...`), submit → `telegramService.startLogin(apiId, apiHash, phone)` → step `code`. Errori mostrati sotto il form.
- Step `code`: input codice + input password 2FA (visibile sempre, etichetta "Password 2FA, solo se attiva"), submit → `telegramService.completeLogin(code, password || undefined)` → step `groups`.
- Step `groups`: rende `<GroupPicker />` + bottone "Fatto" che chiama `setView('home')`.

Ogni submit gestisce l'attesa con uno stato `busy` (il form diventa `inert`, con spinner; MAI `disabled` sui bottoni).

- [ ] **Step 3: GroupPicker**

`src/components/GroupPicker.tsx`:

```tsx
import { useEffect, useState } from 'react'
import { useLiveQuery } from 'dexie-react-hooks'
import { db } from '../lib/db/db'
import { telegramService } from '../lib/telegram/service'
import styles from './GroupPicker.module.css'

export function GroupPicker() {
  const [dialogs, setDialogs] = useState<Array<{ id: string; title: string; isGroup: boolean }> | null>(null)
  const [error, setError] = useState('')
  const followed = useLiveQuery(() => db.groups.filter(g => g.followed).toArray(), [], [])

  useEffect(() => {
    telegramService.listDialogs().then(setDialogs).catch(e => setError(String(e)))
  }, [])

  const toggle = async (d: { id: string; title: string }) => {
    const existing = await db.groups.get(d.id)
    if (existing?.followed) await db.groups.update(d.id, { followed: false })
    else await db.groups.put({ id: d.id, title: d.title, followed: true, lastSyncedMsgId: existing?.lastSyncedMsgId ?? 0, lastDigestAt: existing?.lastDigestAt ?? 0 })
  }

  if (error) return <p className={styles.error}>{error}</p>
  if (!dialogs) return <p>Carico i tuoi gruppi...</p>
  const followedIds = new Set((followed ?? []).map(g => g.id))
  return (
    <ul className={styles.list}>
      {dialogs.filter(d => d.isGroup).map(d => (
        <li key={d.id}>
          <label>
            <input type="checkbox" checked={followedIds.has(d.id)} onChange={() => toggle(d)} />
            {d.title}
          </label>
        </li>
      ))}
    </ul>
  )
}
```

- [ ] **Step 4: App.tsx switch**

`src/App.tsx`: `AppProvider` + switch su `view`: `onboarding` → `<Onboarding />`, `home` → placeholder `<p>home</p>` (Task 7), `settings` → placeholder (Task 8). Finché `authChecked` è false: splash con logo/nome.

- [ ] **Step 5: Verifica manuale e checkpoint**

Run: `npm run dev`, poi nel browser: login completo con account reale (numero, codice, eventuale 2FA), lista gruppi visibile, toggle persistente al reload (IndexedDB → tab Application dei devtools). `npm run lint` PASS. Checkpoint utente per commit.

---

### Task 7: Home UI (digest all'apertura + storico)

**Files:**
- Create: `src/components/Home.tsx`, `src/components/Home.module.css`, `src/components/SummaryCard.tsx`, `src/components/SummaryCard.module.css`, `src/hooks/useOpenDigest.ts`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `syncFollowedGroups`, `runOpenDigest` (Task 5), `db` (Task 2), `pruneOldMessages` (Task 2), `getSettings` (Task 2), `TelegramAuthError` (Task 4), `useApp` (Task 6).
- Produces:
  - `useOpenDigest(): { phase: 'idle' | 'syncing' | 'digesting' | 'done' | 'error'; error: string }` hook che al mount esegue: pruning → sync → digest
  - `<Home />`: header con nome app + bottone Settings, stato del digest, storico summaries
  - `<SummaryCard summary={Summary} groupTitle={string} />`: card con tipo, gruppo, periodo, testo markdown

- [ ] **Step 1: Hook useOpenDigest**

`src/hooks/useOpenDigest.ts`:

```ts
import { useEffect, useRef, useState } from 'react'
import { pruneOldMessages } from '../lib/db/pruning'
import { getSettings } from '../lib/db/settings'
import { syncFollowedGroups } from '../lib/digest/sync'
import { runOpenDigest } from '../lib/digest/runDigest'
import { TelegramAuthError } from '../lib/telegram/service'
import { useApp } from '../context/AppContext'

export type DigestPhase = 'idle' | 'syncing' | 'digesting' | 'done' | 'error'

export function useOpenDigest(): { phase: DigestPhase; error: string } {
  const [phase, setPhase] = useState<DigestPhase>('idle')
  const [error, setError] = useState('')
  const ran = useRef(false)
  const { setView } = useApp()

  useEffect(() => {
    if (ran.current) return
    ran.current = true
    ;(async () => {
      try {
        const s = await getSettings()
        await pruneOldMessages(Date.now(), s.retentionDays)
        setPhase('syncing')
        await syncFollowedGroups()
        setPhase('digesting')
        await runOpenDigest(Date.now())
        setPhase('done')
      } catch (e) {
        if (e instanceof TelegramAuthError) { setView('onboarding'); return }
        setError(String(e))
        setPhase('error')
      }
    })()
  }, [setView])

  return { phase, error }
}
```

- [ ] **Step 2: SummaryCard**

`src/components/SummaryCard.tsx`: mostra badge tipo ("Digest" o "Domanda"), titolo gruppo (o "Tutti i gruppi" se `groupId === null`), periodo formattato `gg/mm - gg/mm`, la domanda se presente (in grassetto), poi `<ReactMarkdown>{summary.text}</ReactMarkdown>`, footer piccolo con `model` e data. Stile card: `background: var(--surface)`, bordo, radius, padding.

- [ ] **Step 3: Home**

`src/components/Home.tsx`:

```tsx
import { useLiveQuery } from 'dexie-react-hooks'
import { db } from '../lib/db/db'
import { useOpenDigest } from '../hooks/useOpenDigest'
import { useApp } from '../context/AppContext'
import { SummaryCard } from './SummaryCard'
import styles from './Home.module.css'

export function Home() {
  const { phase, error } = useOpenDigest()
  const { setView } = useApp()
  const summaries = useLiveQuery(() => db.summaries.orderBy('createdAt').reverse().limit(50).toArray(), [], [])
  const groups = useLiveQuery(() => db.groups.toArray(), [], [])
  const titleOf = (id: string | null) => id === null ? 'Tutti i gruppi' : groups?.find(g => g.id === id)?.title ?? id

  return (
    <div className={styles.page}>
      <header className={styles.header}>
        <h1>tg-digest</h1>
        <button onClick={() => setView('settings')}>Impostazioni</button>
      </header>
      {phase === 'syncing' && <p className={styles.status}>Sync dei gruppi in corso...</p>}
      {phase === 'digesting' && <p className={styles.status}>Genero il digest...</p>}
      {phase === 'error' && <p className={styles.error}>{error}</p>}
      {/* AskBox arriva nel Task 8 */}
      <div className={styles.feed}>
        {(summaries ?? []).map(s => <SummaryCard key={s.id} summary={s} groupTitle={titleOf(s.groupId)} />)}
      </div>
    </div>
  )
}
```

In `App.tsx` sostituire il placeholder `home` con `<Home />`.

- [ ] **Step 4: Verifica manuale e checkpoint**

Run: `npm run dev` con Ollama avviato (`OLLAMA_ORIGINS=* ollama serve`): all'apertura parte il sync, arriva almeno un digest per un gruppo con messaggi recenti, il digest resta al reload (e non si rigenera se non ci sono messaggi nuovi). `npm run lint && npm test` PASS. Checkpoint utente per commit.

---

### Task 8: Q&A UI + Settings UI

**Files:**
- Create: `src/components/AskBox.tsx`, `src/components/AskBox.module.css`, `src/components/Settings.tsx`, `src/components/Settings.module.css`
- Modify: `src/components/Home.tsx`, `src/App.tsx`

**Interfaces:**
- Consumes: `askQuestion` (Task 5), `db`, `getSettings`, `setSetting`, `DEFAULT_SETTINGS` (Task 2), `telegramService` (Task 4), `useApp` (Task 6).
- Produces:
  - `<AskBox />`: textarea domanda, select gruppo ("Tutti" + gruppi seguiti), select periodo (24h / 7 giorni / 30 giorni), submit → `askQuestion(...)`; il risultato appare nel feed via `useLiveQuery` (nessun output locale)
  - `<Settings />`: form provider (select ollama/anthropic), modello (testo), API key Anthropic (password, visibile solo con provider anthropic), URL Ollama (visibile solo con provider ollama), profilo (textarea con placeholder d'esempio "UX engineer che vuole prodottizzare servizi..."), retention giorni (number); salvataggio immediato per campo (`onBlur`/`onChange` → `setSetting`); bottone "Torna alla home"; sezione "Account" con bottone "Logout" → `telegramService.logout()` + `setView('onboarding')`

- [ ] **Step 1: AskBox**

`src/components/AskBox.tsx`:

```tsx
import { useState } from 'react'
import { useLiveQuery } from 'dexie-react-hooks'
import { db } from '../lib/db/db'
import { askQuestion } from '../lib/digest/runDigest'
import styles from './AskBox.module.css'

const PERIODS = [
  { label: 'Ultime 24 ore', ms: 86_400_000 },
  { label: 'Ultimi 7 giorni', ms: 7 * 86_400_000 },
  { label: 'Ultimi 30 giorni', ms: 30 * 86_400_000 },
]

export function AskBox() {
  const groups = useLiveQuery(() => db.groups.filter(g => g.followed).toArray(), [], [])
  const [question, setQuestion] = useState('')
  const [groupId, setGroupId] = useState('all')
  const [periodMs, setPeriodMs] = useState(PERIODS[1].ms)
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState('')

  const submit = async () => {
    if (!question.trim() || busy) return
    setBusy(true); setError('')
    try {
      const ids = groupId === 'all' ? (groups ?? []).map(g => g.id) : [groupId]
      const now = Date.now()
      await askQuestion({ groupIds: ids, from: now - periodMs, to: now, question: question.trim() })
      setQuestion('')
    } catch (e) {
      setError(String(e))
    } finally {
      setBusy(false)
    }
  }

  return (
    <div className={styles.box} inert={busy || undefined}>
      <textarea value={question} onChange={e => setQuestion(e.target.value)}
        placeholder="Chiedi qualcosa sui tuoi gruppi..." rows={2} />
      <div className={styles.controls}>
        <select value={groupId} onChange={e => setGroupId(e.target.value)}>
          <option value="all">Tutti i gruppi</option>
          {(groups ?? []).map(g => <option key={g.id} value={g.id}>{g.title}</option>)}
        </select>
        <select value={periodMs} onChange={e => setPeriodMs(Number(e.target.value))}>
          {PERIODS.map(p => <option key={p.ms} value={p.ms}>{p.label}</option>)}
        </select>
        <button onClick={submit}>{busy ? 'Sto pensando...' : 'Chiedi'}</button>
      </div>
      {error && <p className={styles.error}>{error}</p>}
    </div>
  )
}
```

Montare `<AskBox />` in `Home.tsx` sopra il feed (al posto del commento del Task 7).

- [ ] **Step 2: Settings**

`src/components/Settings.tsx`: carica i settings con `useEffect` + `getSettings()` in uno stato locale, ogni campo salva con `setSetting` su change. Se il provider è `ollama` mostrare hint sotto l'URL: "Avvia Ollama con OLLAMA_ORIGINS=* ollama serve". Montare in `App.tsx` al posto del placeholder `settings`.

- [ ] **Step 3: Verifica manuale e checkpoint**

Run: `npm run dev`: fare una domanda su un gruppo reale con Ollama, verificare che la risposta appaia nel feed e persista al reload; cambiare provider ad anthropic con una key vera e rifare una domanda; logout riporta all'onboarding. `npm run lint && npm test` PASS. Checkpoint utente per commit.

---

### Task 9: Rifinitura errori + PWA + README

**Files:**
- Create: `README.md`, `public/` icone PWA (basta un PNG 512 e uno 192 generati a mano o con uno script)
- Modify: `src/components/Home.tsx` (banner errori), `src/hooks/useOpenDigest.ts` (solo se emergono difetti dal collaudo)

**Interfaces:**
- Consumes: tutto quanto sopra.
- Produces: app collaudata end-to-end, installabile come PWA, README con setup.

- [ ] **Step 1: Banner errori LLM**

In `Home.tsx`: se `phase === 'error'` e il messaggio contiene `fetch`/`Failed to fetch` e il provider è ollama, mostrare il banner dedicato: "Ollama non raggiungibile. Avvialo con: OLLAMA_ORIGINS=* ollama serve" (leggere il provider con `getSettings()` nell'hook e restituirlo). Altrimenti mostrare l'errore raw.

- [ ] **Step 2: README**

`README.md` con: cos'è, prerequisiti (Node, account Telegram, api_id/api_hash da my.telegram.org, Ollama o API key Anthropic), avvio (`npm i && npm run dev`), nota CORS Ollama, nota privacy (tutto resta nel browser tranne le chiamate LLM al provider scelto), limiti noti (digest solo ad app aperta, solo testo).

- [ ] **Step 3: Collaudo end-to-end**

Percorso completo da browser pulito (profilo incognito): onboarding → login → scelta 2 gruppi → digest all'apertura → domanda → cambio provider → reload → nessun re-login, digest non rigenerato senza messaggi nuovi. `npm run build && npm run lint && npm test` PASS.

- [ ] **Step 4: Checkpoint utente**

Segnalare il completamento; commit finale e eventuale deploy statico (Netlify come forfettAIro) a discrezione dell'utente.
